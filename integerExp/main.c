#include <stdio.h>

int main() {
    /*signed int i;
    signed i;  // o compilador assume que a variavel é  inteira
    unsigned i;//o compilador assume que a variavel é  inteira
    long i;
    long int i;
    long long i;//conlador assume long long int i
    */
    return 0;
}
